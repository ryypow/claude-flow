# Architecture Overview

Comprehensive technical overview of Claude Flow v2 Docker Swarm deployment architecture.

## 🏗️ System Architecture

### High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        External Access Layer                    │
├─────────────────────────────────────────────────────────────────┤
│  Load Balancer (Optional)     │  External Users & Systems      │
│  - Nginx/Traefik             │  - Web Browsers                │
│  - SSL Termination           │  - API Clients                 │
│  - Rate Limiting              │  - Monitoring Systems          │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Docker Swarm Ingress Network                │
├─────────────────────────────────────────────────────────────────┤
│  Port Mappings:                                                 │
│  - 4000:3000  (Web UI + WebSocket)                            │
│  - 4001:3001  (API Server)                                    │
│  - 4080:8080  (Tools & Utilities)                             │
│  - 9000:9000  (Metrics - Optional)                            │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Claude Flow Service Layer                  │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │   Replica 1     │  │   Replica 2     │  │   Replica N     │  │
│  │                 │  │                 │  │                 │  │
│  │ ┌─────────────┐ │  │ ┌─────────────┐ │  │ ┌─────────────┐ │  │
│  │ │   Web UI    │ │  │ │   Web UI    │ │  │ │   Web UI    │ │  │
│  │ │  (Port 3000)│ │  │ │  (Port 3000)│ │  │ │  (Port 3000)│ │  │
│  │ └─────────────┘ │  │ └─────────────┘ │  │ └─────────────┘ │  │
│  │ ┌─────────────┐ │  │ ┌─────────────┐ │  │ ┌─────────────┐ │  │
│  │ │ API Server  │ │  │ │ API Server  │ │  │ │ API Server  │ │  │
│  │ │  (Port 3001)│ │  │ │  (Port 3001)│ │  │ │  (Port 3001)│ │  │
│  │ └─────────────┘ │  │ └─────────────┘ │  │ └─────────────┘ │  │
│  │ ┌─────────────┐ │  │ ┌─────────────┐ │  │ ┌─────────────┐ │  │
│  │ │ WebSocket   │ │  │ │ WebSocket   │ │  │ │ WebSocket   │ │  │
│  │ │   Server    │ │  │ │   Server    │ │  │ │   Server    │ │  │
│  │ └─────────────┘ │  │ └─────────────┘ │  │ └─────────────┘ │  │
│  │ ┌─────────────┐ │  │ ┌─────────────┐ │  │ ┌─────────────┐ │  │
│  │ │Claude Flow  │ │  │ │Claude Flow  │ │  │ │Claude Flow  │ │  │
│  │ │  Runtime    │ │  │ │  Runtime    │ │  │ │  Runtime    │ │  │
│  │ └─────────────┘ │  │ └─────────────┘ │  │ └─────────────┘ │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                        Data & Storage Layer                     │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ Application │  │Configuration│  │   Project   │             │
│  │    Data     │  │    Data     │  │    Data     │             │
│  │  (Volume)   │  │  (Volume)   │  │  (Volume)   │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │    Logs     │  │   Secrets   │  │   Cache     │             │
│  │  (Volume)   │  │  (Docker)   │  │ (Optional)  │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Infrastructure Layer                        │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ Manager     │  │  Worker     │  │  Worker     │             │
│  │   Node      │  │   Node      │  │   Node      │             │
│  │             │  │             │  │             │             │
│  │ ┌─────────┐ │  │ ┌─────────┐ │  │ ┌─────────┐ │             │
│  │ │ Docker  │ │  │ │ Docker  │ │  │ │ Docker  │ │             │
│  │ │ Engine  │ │  │ │ Engine  │ │  │ │ Engine  │ │             │
│  │ └─────────┘ │  │ └─────────┘ │  │ └─────────┘ │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
└─────────────────────────────────────────────────────────────────┘
```

## 🔧 Component Architecture

### 1. Web UI Layer

**Technology Stack:**
- Frontend: Modern web technologies (HTML5, CSS3, JavaScript)
- Real-time Communication: WebSocket with JSON-RPC 2.0
- Responsive Design: Mobile and desktop compatible

**Components:**
```
┌─────────────────────────────────────┐
│            Web UI (Port 3000)       │
├─────────────────────────────────────┤
│  ┌─────────────┐ ┌─────────────────┐ │
│  │  Dashboard  │ │  Agent Manager  │ │
│  │             │ │                 │ │
│  │ - Status    │ │ - Agent List    │ │
│  │ - Metrics   │ │ - Task Queue    │ │
│  │ - Alerts    │ │ - Performance   │ │
│  └─────────────┘ └─────────────────┘ │
│  ┌─────────────┐ ┌─────────────────┐ │
│  │   Console   │ │  Configuration  │ │
│  │             │ │                 │ │
│  │ - Terminal  │ │ - Settings      │ │
│  │ - Logs      │ │ - API Keys      │ │
│  │ - Commands  │ │ - Resources     │ │
│  └─────────────┘ └─────────────────┘ │
└─────────────────────────────────────┘
```

### 2. API Server Layer

**Technology Stack:**
- Runtime: Node.js with Express/Fastify
- Protocol: RESTful API with JSON
- Authentication: API Key based
- Validation: Request/response validation

**API Structure:**
```
┌─────────────────────────────────────┐
│           API Server (Port 3001)    │
├─────────────────────────────────────┤
│  ┌─────────────┐ ┌─────────────────┐ │
│  │   Health    │ │   Agent API     │ │
│  │             │ │                 │ │
│  │ - /status   │ │ - /agents       │ │
│  │ - /info     │ │ - /agents/:id   │ │
│  │ - /metrics  │ │ - /agents/tasks │ │
│  └─────────────┘ └─────────────────┘ │
│  ┌─────────────┐ ┌─────────────────┐ │
│  │   Task API  │ │   Swarm API     │ │
│  │             │ │                 │ │
│  │ - /tasks    │ │ - /swarm/nodes  │ │
│  │ - /tasks/:id│ │ - /swarm/scale  │ │
│  │ - /queue    │ │ - /swarm/status │ │
│  └─────────────┘ └─────────────────┘ │
└─────────────────────────────────────┘
```

### 3. WebSocket Communication

**Protocol Implementation:**
- **Standard**: JSON-RPC 2.0 over WebSocket
- **Features**: Bidirectional, real-time, structured messaging
- **Reliability**: Auto-reconnection, heartbeat, error handling

**Message Flow:**
```
Client                    Server
  │                         │
  │ ── Connection Request ──→ │
  │ ←── Connection Accept ─── │
  │                         │
  │ ── Initialize ──────────→ │
  │ ←── Capabilities ──────── │
  │                         │
  │ ← Agent Status Updates ── │
  │ ← Task Progress ────────── │
  │ ← System Metrics ───────── │
  │                         │
  │ ── Commands ────────────→ │
  │ ←── Responses ──────────── │
  │                         │
  │ ◄─── Heartbeat ────────→ │
```

### 4. Claude Flow Runtime

**Core Components:**
- **Agent Manager**: Coordinates 64+ specialized AI agents
- **Task Scheduler**: Distributes and manages tasks
- **Hive Mind**: Collective intelligence coordination
- **SPARC Engine**: 17 development pattern modes

**Agent Architecture:**
```
┌─────────────────────────────────────┐
│         Claude Flow Runtime         │
├─────────────────────────────────────┤
│  ┌─────────────┐ ┌─────────────────┐ │
│  │Agent Manager│ │  Task Scheduler │ │
│  │             │ │                 │ │
│  │ - Discovery │ │ - Queue Mgmt    │ │
│  │ - Lifecycle │ │ - Priority      │ │
│  │ - Resources │ │ - Distribution  │ │
│  └─────────────┘ └─────────────────┘ │
│  ┌─────────────┐ ┌─────────────────┐ │
│  │ Hive Mind   │ │  SPARC Engine   │ │
│  │             │ │                 │ │
│  │ - Memory    │ │ - Patterns      │ │
│  │ - Context   │ │ - Workflows     │ │
│  │ - Learning  │ │ - Templates     │ │
│  └─────────────┘ └─────────────────┘ │
└─────────────────────────────────────┘
```

## 🌐 Network Architecture

### Overlay Networks

**claude-flow-network (10.1.0.0/24):**
- **Purpose**: Internal service communication
- **Type**: Encrypted overlay network
- **Features**: Service discovery, load balancing
- **Security**: Isolated from external networks

**Network Flow:**
```
Internet ──→ Ingress Network ──→ Overlay Network ──→ Service Containers
    │              │                    │                    │
    │              │                    │                    │
    ▼              ▼                    ▼                    ▼
External       Port Mapping      Service Discovery    Application
Traffic       (4000→3000)         (DNS Names)           Logic
```

### Service Discovery

**Docker Swarm DNS:**
- Service names resolve to virtual IPs
- Load balancing across healthy replicas
- Automatic failover on container failure

**Example Resolution:**
```bash
# Inside container
ping claude-flow-alpha
# Resolves to: 10.1.0.x (Virtual IP)

# Service endpoints
curl http://claude-flow-alpha:3000/api/status
curl http://claude-flow-alpha:3001/api/agents
```

## 💾 Data Architecture

### Storage Strategy

**Volume Types:**
1. **Application Data** (`claude_flow_data`)
   - Claude Flow state and runtime data
   - Agent configurations and memory
   - Persistent across container restarts

2. **Configuration Data** (`claude_flow_config`) 
   - User settings and preferences
   - Environment configurations
   - API key references

3. **Project Data** (`claude_projects`)
   - User projects and workspaces
   - Development artifacts
   - Code repositories

4. **Log Data** (`claude_logs`)
   - Application logs
   - Audit trails
   - Debug information

### Data Flow

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  User Input │───→│  API Layer  │───→│   Runtime   │
└─────────────┘    └─────────────┘    └─────────────┘
                           │                   │
                           ▼                   ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Logs      │←───│ Validation  │    │   Storage   │
└─────────────┘    └─────────────┘    └─────────────┘
                           │                   │
                           ▼                   ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  Response   │←───│ Processing  │←───│   Agents    │
└─────────────┘    └─────────────┘    └─────────────┘
```

## 🔐 Security Architecture

### Multi-Layer Security

**1. Infrastructure Security:**
- Docker Swarm encrypted networks
- Non-root container execution (UID 1001)
- Resource limits and constraints
- Read-only file systems where possible

**2. Application Security:**
- API key authentication
- Request rate limiting
- Input validation and sanitization
- CORS policy enforcement

**3. Data Security:**
- Docker secrets for sensitive data
- Encrypted overlay networks
- Secure volume mounts
- Audit logging

### Security Boundaries

```
┌─────────────────────────────────────┐ ← External Firewall
│              Internet               │
└─────────────────────────────────────┘
                    │
┌─────────────────────────────────────┐ ← Ingress Network
│          Docker Swarm               │
│                                     │
│ ┌─────────────────────────────────┐ │ ← Overlay Network (Encrypted)
│ │         Service Layer           │ │
│ │                                 │ │
│ │ ┌─────────────────────────────┐ │ │ ← Container Security
│ │ │      Application            │ │ │
│ │ │   (Non-root user)           │ │ │
│ │ └─────────────────────────────┘ │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
                    │
┌─────────────────────────────────────┐ ← Volume Security
│          Persistent Data            │
└─────────────────────────────────────┘
```

## ⚡ Performance Architecture

### Scaling Strategy

**Horizontal Scaling:**
- Multiple service replicas
- Load balancing across replicas
- Automatic failure recovery
- Rolling updates with zero downtime

**Vertical Scaling:**
- Configurable resource limits
- Dynamic resource allocation
- Memory and CPU optimization
- Performance monitoring

### Performance Optimization

**1. Application Level:**
```javascript
// Connection pooling
const pool = new ConnectionPool({
  max: 100,
  min: 10,
  keepAlive: true
});

// Caching strategy
const cache = new LRUCache({
  max: 1000,
  ttl: 300000  // 5 minutes
});

// Async processing
const taskQueue = new Queue('claude-flow-tasks', {
  concurrency: 32
});
```

**2. Infrastructure Level:**
```yaml
# Resource optimization
resources:
  limits:
    memory: 16G
    cpus: '8.0'
  reservations:
    memory: 8G
    cpus: '4.0'

# Health checks
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:3000/api/status"]
  interval: 30s
  timeout: 10s
  retries: 3
```

## 📊 Monitoring Architecture

### Metrics Collection

**System Metrics:**
- CPU, memory, disk usage
- Network I/O statistics
- Container resource usage
- Docker service health

**Application Metrics:**
- API response times
- WebSocket connection counts
- Agent task completion rates
- Error rates and types

**Custom Metrics:**
- Claude Flow specific metrics
- Agent performance data
- Task queue statistics
- User activity metrics

### Observability Stack

```
┌─────────────────────────────────────┐
│            Monitoring               │
├─────────────────────────────────────┤
│  ┌─────────────┐ ┌─────────────────┐ │
│  │ Prometheus  │ │    Grafana      │ │
│  │ (Metrics)   │ │ (Visualization) │ │
│  └─────────────┘ └─────────────────┘ │
│  ┌─────────────┐ ┌─────────────────┐ │
│  │    Logs     │ │    Alerting     │ │
│  │ (ELK Stack) │ │ (AlertManager)  │ │
│  └─────────────┘ └─────────────────┘ │
└─────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────┐
│         Claude Flow Services        │
└─────────────────────────────────────┘
```

## 🔄 Deployment Architecture

### CI/CD Pipeline

**Build Process:**
1. Code checkout and validation
2. Docker image building
3. Security scanning
4. Unit and integration testing
5. Performance testing
6. Image publishing

**Deployment Process:**
1. Rolling update strategy
2. Health check validation
3. Traffic migration
4. Rollback capability
5. Monitoring activation

### Environment Strategy

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│Development  │───→│   Staging   │───→│ Production  │
│             │    │             │    │             │
│ - Local     │    │ - Pre-prod  │    │ - Live      │
│ - Testing   │    │ - Validation│    │ - Scaled    │
│ - Debug     │    │ - Performance│    │ - Monitored │
└─────────────┘    └─────────────┘    └─────────────┘
```

## 🔧 Configuration Architecture

### Configuration Management

**Environment Variables:**
- Runtime configuration
- Feature flags
- Resource limits
- Integration settings

**Docker Secrets:**
- API keys and tokens
- SSL certificates
- Database passwords
- Service credentials

**Volume Configuration:**
- Application settings
- User preferences
- Runtime state
- Temporary data

### Configuration Hierarchy

```
Default Values
    ↓
Environment Variables
    ↓
Docker Secrets
    ↓
Volume Configurations
    ↓
Runtime Settings
```

## 📚 Related Documentation

- [Installation Guide](../deployment/installation.md) - Setup instructions
- [API Reference](../api/README.md) - API documentation
- [Troubleshooting](../troubleshooting/README.md) - Problem solving
- [Performance Guide](../deployment/performance.md) - Optimization tips

---

**Need more details?** Explore specific component documentation or open an [issue](../../issues) for clarification.
